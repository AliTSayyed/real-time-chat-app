import { Customdate2Pipe } from './customdate2.pipe';

describe('Customdate2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Customdate2Pipe();
    expect(pipe).toBeTruthy();
  });
});
